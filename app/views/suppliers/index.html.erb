<h1>Fornecedores</h1>
<% if flash[:notice] %>
  <p style='color: green'><%= flash[:notice]%></p>
<% end %>

<%= link_to 'Cadastrar Fornecedor', new_supplier_path %>

<% @suppliers.each do |s| %>
  <div>
    <p><%=link_to s.fantasy_name, s %></p>
    <p>CNPJ: <%= s.cnpj%></p>
    <p>Telephone: <%= s.phone%></p>
  </div>
<% end %>

<% if @suppliers.empty? %>
  <p>Não existem fornecedores cadastrados</p>
<% end %>